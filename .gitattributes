# This repo is usually checked out on Windows with core.autocrlf=true, which
# would turn shell scripts into CRLF on checkout. A CRLF script fails on the
# target hosts with "bad interpreter: No such file or directory", so force LF
# for the test helpers. Scoped to this directory to leave upstream files alone.
packaging/test/*.sh text eol=lf
